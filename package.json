{
  "name": "bmr-calculator-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo root for the BMR calculator library and its demo site.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter site dev",
    "build": "pnpm --filter bmr-calculator build && pnpm --filter site build",
    "test": "pnpm --filter bmr-calculator test"
  }
}
